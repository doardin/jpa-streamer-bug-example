spring:
  datasource:
    url: jdbc:mysql://localhost:8889/example
    username: root
    password: root
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
      dialect: org.hibernate.dialect.MySQL8Dialect